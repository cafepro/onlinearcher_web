<%= form_for(@arrow) do |f| %>
  <% if @arrow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arrow.errors.count, "error") %> prohibited this arrow from being saved:</h2>

      <ul>
      <% @arrow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :score_id %><br>
    <%= f.number_field :score_id %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
